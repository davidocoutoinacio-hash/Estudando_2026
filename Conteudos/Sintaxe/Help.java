public class Help {

       public void iniciante(){
        
        //Apoio para aprender Sintaxe


        // Declaração de Variável
        //<tipo> identificador = valor;                    

        // Vetor Array                                          int[] numeros = {1, 2, 3};
        //<tipo>[] identificador = {valores};                   String[] nomes = {"Ana", "João", "Davi"};
        //ArrayList<tipo> objeto = new ArrayList<>();     

                                                                //ArrayList<String> tarefas = new ArrayList<>();
                                                                //tarefas.add("String");
                                                                // System.out.println(tarefas.get(0));
                                                                //System.out.println(tarefas.size());
                                                                //System.out.println(tarefas);



        // Estrutura condicional if
        //if (condicao) {                                       if (idade >= 18) {
        // código                                                System.out.println("Maior de idade");
        //}                                                      }

        // if com else
        //if (condicao) {
        //} else {
        //}

        //Estrutura switch (variavel) {
        //case valor:
        //break;
        //default:
        //}

        //Laço for                                                for (int i = 0; i < 5; i++) {
        //for (inicializacao; condicao; incremento) {             System.out.println(i);
        //}                                                       }

        //Laço while
        //while (condicao) {
        //}

        //Laço do while
        //do {
        //} while (condicao);

        //Metodo                                              
        //<modificador> <retorno> nomeMetodo(parametros) {        public int somar(int a, int b) {
        //return valor;                                           return a + b;
        //}                                                       }

        //Classes
        //class NomeDaClasse {
        //}

        //Metodo main
        //public static void main(String[] args) {
        //}

        //Criar objeto (new)
        //Classe objeto = new Classe();

        //Construtor
        //public Classe(parametros) {
        //}

        //Encapsulamento
        //private tipo variavel;
        //public tipo getVariavel();
        //public void setVariavel(tipo valor);
        

        //Palavras chave de acesso;
        //public
        //private
        //protected

        //Herança
        //class Filho extends Pai {
        //}

        //Interface
        //interface Nome {
        //}

        //Inplementar Interface
        //class Classe implements Interface {
        //}

        //Tratamento de excessão
        //try {
        //} catch (Exception e) {
        //} finally {
        //}

        //importação
        //import pacote.Classe;


        /*   Palavras reservadas mais importantes
        class
        public
        private
        protected
        static
        final
        if
        else
        switch
        case
        for
        while
        do
        break
        continue
        return
        new
        this
        extends
        implements
        try
        catch
        finally
        throw
        throws
        */
    }

        public void intermediario(){

        //final (imutabilidade / segurança)
        //final <tipo> identificador = valor;   final int PORTA = 8080;

        //abstract (contratos e templates)
        //Sintaxe (classe):
        //abstract class Classe {
        //}
        //Sintaxe (método):
        //abstract <retorno> metodo();

        //super (acesso à classe pai)
        //super.metodo();
        //super( argumentos );

        //static (membro da classe)
        //static <tipo> identificador;
        //static <retorno> metodo();

        //enum (tipos seguros)
        //enum Nome {
        //VALOR1, VALOR2
        //}

        //synchronized (concorrência)

        //synchronized (objeto) {
        //}
        //           ou
        //synchronized retorno metodo() {
        //}

        //volatile (visibilidade entre threads)
        //volatile <tipo> identificador;

        //transient (serialização)
        //transient <tipo> identificador;

        //native (JNI)
        //native retorno metodo(parametros);

        //strictfp (precisão matemática)
        //strictfp class Classe {
        //}

        //assert (validação em tempo de execução)
        //assert condicao;
        //assert condicao : mensagem;

        //instanceof (verificação de tipo)
        //obj instanceof Tipo variavel

        //record (objetos imutáveis)
        //record Nome(tipo campo1, tipo campo2) { }

        //sealed (controle de herança)
        //sealed class Classe permits A, B { }

        //non-sealed (libera herança)
        //non-sealed class Classe extends Pai { }

        //var (inferência de tipo)
        //var identificador = valor;
        
        //yield (switch expression)
        //yield valor;

        //throws (propagação de exceções)
        //retorno metodo() throws Excecao;

        //default (método em interface)
        //default retorno metodo() {
        //}
        
        //module (Java Platform Module System)
        //module nome {
        //requires modulo;
        //exports pacote;
        //}

        //opens (reflexão em módulos)
        //opens pacote;

        //provides / uses (SPI)
        //provides Interface with Implementacao;
        //uses Interface;

        /*     Palavras reservadas avançadas que poucos dominam
        volatile
        transient
        native
        strictfp
        sealed
        non-sealed
        record
        yield
        assert
        module
        opens
        provides
        uses
             */


















        }


 }
    

